# OpenAI API Configuration (REQUIRED)
OPENAI_API_KEY=your_openai_api_key_here

# InterSystems IRIS Database Configuration
# These are the default values for the FHIR-AI-Hackathon-Kit Docker setup
# Only change if using a different IRIS instance
IRIS_HOST=localhost
IRIS_PORT=32782
IRIS_NAMESPACE=USER
IRIS_USERNAME=_SYSTEM
IRIS_PASSWORD=ISCDEMO

# Note: Model configuration (embedding_model, openai_model, RAG parameters)
# should be changed in backend/config.py, not in this .env file
